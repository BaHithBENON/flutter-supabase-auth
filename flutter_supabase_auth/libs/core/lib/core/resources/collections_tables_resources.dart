// ignore_for_file: constant_identifier_names

/// This class contains the names of the collections and tables in the database.
/// 
/// Each constant represents the name of a collection or table.
class CollectionsTablesResources {
  // Collection: profiles
  /// The name of the collection that stores the profiles.
  static const String PROFILES = "profiles";

  // Table: users_documents
  /// The name of the table that stores the users' documents.
  static const String USERS_DOCUMENTS = "users_documents";

  // Table: notifications
  /// The name of the table that stores the notifications.
  static const String NOTIFICATIONS = "notifications";

  // Table: pricings
  /// The name of the table that stores the pricings.
  static const String PRICINGS = "pricings";

  // Table: subscriptions
  /// The name of the table that stores the subscriptions.
  static const String SUBSCRIPTIONS = "subscriptions";

  // Table: news
  /// The name of the table that stores the news.
  static const String NEWS = "news";

  // Table: articles_sections
  /// The name of the table that stores the articles sections.
  static const String ARTICLES_SECTIONS = "articles_sections";

  // Table: chat_rooms
  /// The name of the table that stores the chat rooms.
  static const String CHAT_ROOMS = "chat_rooms";

  // Table: messages
  /// The name of the table that stores the messages.
  static const String MESSAGES = "messages";

  // Table: posts
  /// The name of the table that stores the posts.
  static const String POSTS = "posts";

  // Table: posts_comments
  /// The name of the table that stores the posts comments.
  static const String POSTS_COMMENTS = "posts_comments";

  // Table: deals
  /// The name of the table that stores the deals.
  static const String DEALS = "deals";

  // Table: deals_messages
  /// The name of the table that stores the deals messages.
  static const String DEALS_MESSAGES = "deals_messages";
}
